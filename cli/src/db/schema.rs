table! {
    dapps (id) {
        id -> Integer,
        name -> Text,
        version -> Text,
        install_location -> Text,
        installed_at -> Timestamp,
        updated_at -> Timestamp,
    }
}
